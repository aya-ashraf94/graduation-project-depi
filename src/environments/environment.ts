// ============================================================
// ENVIRONMENT — DEVELOPMENT
// When you build the real backend, just change `apiUrl` here.
// ============================================================

export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api', // ← Point this to your backend
  wsUrl: 'ws://localhost:3000',        // ← WebSocket URL for real-time features
  appName: 'Nafa3ni',
};
